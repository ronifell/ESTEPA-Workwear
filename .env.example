# ESTEPA Workwear — environment variables
# Copy to `.env.local` and fill in the values you actually have.
# Every variable is optional: the site runs with all of them unset.

# Absolute site URL. Used for canonical URLs, hreflang, sitemap and Open Graph.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Contact details
# Leave empty until the real values exist — the UI hides whatever is missing
# instead of rendering a fake placeholder.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_CONTACT_EMAIL=contacto@estepaworkwear.com
NEXT_PUBLIC_CONTACT_PHONE=+54 264 578-6834
# Digits only, international format, no "+" (e.g. 5491131271122)
NEXT_PUBLIC_CONTACT_WHATSAPP=5491131271122
NEXT_PUBLIC_CONTACT_ADDRESS=Los Lapachos 4967, Oeste — Rivadavia, San Juan, Argentina

# Inbox for quotation and contact notifications. Defaults to the public email.
NOTIFY_EMAIL=contacto@estepaworkwear.com
# Optional SMTP. If unset, the app tries /usr/sbin/sendmail on the VPS.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=ESTEPA Workwear <contacto@estepaworkwear.com>

# ---------------------------------------------------------------------------
# Social profiles — only real profiles should be listed.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SOCIAL_INSTAGRAM=
NEXT_PUBLIC_SOCIAL_LINKEDIN=
NEXT_PUBLIC_SOCIAL_FACEBOOK=

# ---------------------------------------------------------------------------
# Commerce
# ---------------------------------------------------------------------------
# Set to "true" once the catalogue has confirmed prices. While it is false the
# storefront collects quotation requests and shows "price to be confirmed"
# instead of inventing amounts.
NEXT_PUBLIC_PRICES_ENABLED=false

# Public path or URL of the downloadable catalogue.
# Defaults to the Spanish FR technical catalogue in /public/documents.
NEXT_PUBLIC_CATALOG_PDF_URL=/documents/ESTEPA-catalogo-tecnico-FR.pdf

# ---------------------------------------------------------------------------
# Google Reviews (footer badge)
# ---------------------------------------------------------------------------
# Public URL of the ESTEPA Google Business profile (reviews tab).
NEXT_PUBLIC_GOOGLE_REVIEWS_URL=
# Optional numeric rating and count, e.g. 4.9 and 28. Leave empty to hide the figure.
NEXT_PUBLIC_GOOGLE_RATING=
NEXT_PUBLIC_GOOGLE_REVIEW_COUNT=

# ---------------------------------------------------------------------------
# Admin panel (/admin)
# Accounts live here and nowhere else. While all three are empty the login
# screen explains that no credentials are configured and nobody can get in.
# ---------------------------------------------------------------------------
# Single account:
ADMIN_EMAIL=admin@estepaworkwear.com
ADMIN_PASSWORD=cambiar-esta-clave

# Extra accounts, "email:password" separated by commas (or one per line).
# A password may contain ":" but not ",".
# ADMIN_USERS=ana@estepaworkwear.com:clave-de-ana, luis@estepaworkwear.com:clave-de-luis

# Signs the admin session cookie. Any long random string (32+ characters).
# If left empty it is derived from the credentials above, which means changing
# a password signs everyone out.
ADMIN_SESSION_SECRET=

# ---------------------------------------------------------------------------
# Database (optional)
# Unset: leads and orders are appended to ./.data/*.ndjson.
# Set:    leads and orders are stored in PostgreSQL through Prisma.
#         Run `npm run db:push` (and `npm run db:seed` for the catalogue).
# ---------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@localhost:5432/estepa
